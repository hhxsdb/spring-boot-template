server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/xueke_springboot?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
    driver-class-name: com.mysql.cj.jdbc.Driver # ????????????????
    username: root
    password: 123456
  devtools:
    restart:
      enabled: true  # ???????
      additional-paths: src/main/java # ????
      exclude: WEB-INF/**
  freemarker:
    cache: false    # ??????????????

logging:
  level:
    com.wms: debug